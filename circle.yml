machine:
  pre:
    - mkdir -p ${CIRCLE_TEST_REPORTS}/nosetests/

dependencies:
  override:
    - pip install tox tox-pyenv
    - pip install ./
    - pyenv local 2.7.10 3.6.0

test:
  override:
    - smbedit -h
    - tox
