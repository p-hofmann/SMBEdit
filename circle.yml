#  post:
#    - pyenv global 2.7.10 3.6.0  # Version of python to use
machine:

#  python:
#    version: 3.6.0

  post:
    - pyenv global 2.6.8 2.7.10 3.3.3 3.4.3 3.5.0 3.6.0  # Version of python to use

dependencies:
  override:
    - pip install tox tox-pyenv
    - pyenv local 2.6.8 2.7.10 3.3.3 3.4.3 3.5.0 3.6.0
