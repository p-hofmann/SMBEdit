machine:
  pre:
    - mkdir -p ${CIRCLE_TEST_REPORTS}/nosetests/

dependencies:
  override:
#    - sudo apt-get update; sudo apt-get install python-tk python3-tk tk tk-dev
#    - pyenv install -f 2.7.10
#    - pyenv install -f 3.6.0
    - pip install tox tox-pyenv cx_Freeze
    - pip install -I "git+https://github.com/p-hofmann/PyVoxelizer.git"
    - pip install -I ./
    - pyenv local 2.7.10 3.6.2

test:
  override:
    - smbedit -h
    - smbedit unittests/test_blueprints/B_Box.sment
    - tox
#    - python make.py build
